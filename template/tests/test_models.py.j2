"""Test the models for {{ project_name }}."""

from {{ package_name }} import JSON


def test_json_model() -> None:
    """Test the JSON type alias with a basic payload."""
    payload: JSON = {"status": "ok"}

    assert payload["status"] == "ok"
